server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3308/accountsdb
    username: root
    password: 'root'
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: update
  #    show-sql: true
  application:
    name: "accounts"
  config:
    import: "configserver:http://localhost:8071/"
  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    password: "guest"
management:
  endpoints:
    web:
      exposure:
        include: "*"